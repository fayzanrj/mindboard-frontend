interface GroupProps {
  id: string;
  name: string;
  image: string;
  slug: string;
  // members: string[];
  // invites: string[];
  // createdAt: Date;
  // updatedAt: Date;
}

export default GroupProps;

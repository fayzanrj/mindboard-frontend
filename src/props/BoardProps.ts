interface BoardProps {
  id: string;
  name: string;
  createdBy: string;
  isFavOf: string[];
  lastEditedBy: string;
  image: string;
  // createdAt: Date;
  // updatedAt: Date;
}

export default BoardProps;
